{
  "type": "object",
  "properties": {
    "interaction_description": {
      "type": "string",
      "description": "A second-person scene description under 50 words"
    },
    "player_actions": {
      "type": "array",
      "description": "player actions",
      "items": {
        "type": "object",
        "properties": {
          "action": {
            "type": "string",
            "description": "A short action label under 15 words"
          },
          "exit_flag": {
            "type": "boolean",
            "description": "True if this action ends the current scene"
          }
        },
        "required": ["action", "exit_flag"],
        "additionalProperties": false
      }
    }
  },
  "required": ["interaction_description", "player_actions"],
  "additionalProperties": false
}
